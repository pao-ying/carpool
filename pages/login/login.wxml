<block wx:if="{{authorized}}">
  <button open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo" data-authorized="{{authorized}}">拼车</button>
</block>
<block wx:else>
  <button open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo" data-authorized="{{authorized}}">拼车</button>
</block>
